package Sept.ex_16092024;

public class lab027_compound_assignment {
    public static void main(String[] args) {
        // Compound assignment operator ->The compound assignment operator is the combination of more than one operator.
        // It includes an assignment operator and arithmetic operator or bitwise operator
        //operation is performed between the right operand and the left operand and the resultant assigned to the left operand.
        // +=, -=, *=, %=, /=
        int a = 10;
        //a += 20; // a = a + 20
        //a -= 20; // a = a - 20
        //a /= 10; // a = a / 10
        a *= 10; // a = a * 10

        System.out.println(a);



    }
}
